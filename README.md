# Requirements
```
nvm use 12 && \
npm install --save \
@aws-cdk/aws-ec2 \
@aws-cdk/aws-dynamodb \
@aws-cdk/aws-lambda \
@aws-cdk/aws-apigateway
```
```
cd src && \
npm init -y && \
npm install --save \
aws-sdk \
uuidv4
```